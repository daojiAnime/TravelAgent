[project]
name = "travelagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "agno[openai,tools]>=1.2.15",
    "httpx[socks]>=0.28.1",
    "sqlalchemy>=2.0.40",
]
